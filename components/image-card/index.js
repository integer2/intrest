import Image from 'next/image';
import React from 'react';

const ImageCard = () => {
  return (
    <div className="relative">
      <Image src={'/assets/'}/>
    </div>
  );
};

export default ImageCard;
